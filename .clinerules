# 目标

1. 文件去重工具
2. 支持md5|sha1|sha256|sha512等多种对比算法选择，默认md5
3. ui操作，默认web操作界面，-t|--tui时支持tui界面
4. 对比时并行对比（并发安全），显示进度条
5. 对比结束后按组列出重复文件信息，包含完整文件名、文件大小
6. 用户选择要删除的文件，确认后删除
7. 默认删除到回收站，支持直接删除
8. Web 和 TUI 界面均支持目录浏览器选择目录（含软链接）和手动输入路径

# 技术要求

1. go1.26.3
2. web界面：net/http + Templ + Tailwind CSS + Alpine.js
3. tui界面：Bubble Tea + bubbles（textinput、viewport）

# 其它要求

1. 编码以准确性和人工可读性为首要目标
2. 代码编写坚持KISS原则
3. 核心功能先写测试（可先写空函数占位）再写实现
